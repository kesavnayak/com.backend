import { InterviewCategory } from './interview-category.model';

describe('InterviewCategory', () => {
  it('should create an instance', () => {
    expect(new InterviewCategory()).toBeTruthy();
  });
});
