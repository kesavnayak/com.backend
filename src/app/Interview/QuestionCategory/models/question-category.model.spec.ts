import { QuestionCategory } from './question-category.model';

describe('QuestionCategory', () => {
  it('should create an instance', () => {
    expect(new QuestionCategory()).toBeTruthy();
  });
});
